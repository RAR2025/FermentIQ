name: Register Deployment

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: Production
      url: https://fermentiq.onrender.com
    steps:
      - run: echo "Deployment registered"